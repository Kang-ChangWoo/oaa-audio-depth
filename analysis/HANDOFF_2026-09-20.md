
---

## 7. Parked jobs — deliberately deferred, NOT silently dropped (added 2026-09-20, session 2)

The stage9 dispatcher is dead and left incomplete jobs. Decision: **parked until after fairhop**,
because GPU priority is sweep -> fairhop(at the decided hop) -> micgain -> eco scaling. Parked:

- Window axis (LOW value — win 100 already came back clearly worse, §10 rejection stands):
  `0820_s9_w100_h160_mp3d`, `0820_s9_w200_h160_mp3d`, `0820_s9_w100_h44_rep` (all .oom on the
  9/19 GPU-5 OOM cascade), `0820_s9_w100_h44_mp3d`, `0820_s9_w200_h44_mp3d` (never started).
- Replica-side hop sweep points `0820_s9_h88_rep`, `0820_s9_h22_rep` (never started). The hop
  DECISION runs on MP3D 4ch only; these are appendix completeness, queue them with the window runs.

Also: fairhop dispatcher was deliberately stopped (2026-09-20) until the hop sweep decides the
final hop — restarting it at hop 44 before the decision risks throwing away 12 baseline retrains.
Restart command is in the script header; edit STFT_HOP + run names to the decided hop first.

**Decision (2026-09-20, user-confirmed): micgain stays at hop 44 regardless of the sweep's final
hop.** The scaling curve is self-contained (all 12 points + the future eco-scaling 12 share one
preprocessing), so it stays internally fair; if the benchmark hop ends up != 44, the scaling
section gets a footnote, not a re-run. Do NOT restart micgain at a new hop.
